{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal window capability for the Orbit Code shell",
  "windows": ["main"],
  "permissions": ["core:window:default", "dialog:default"]
}
